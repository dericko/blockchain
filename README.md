Simple blockchain based on github/dvf's post on Hackernoon

# Setup (using pipenv)
pip install pipenv
pipenv --python 3.6
pipenv install Flask==0.12.2 requests==2.18.4

# Tests

# Running
pipenv run python server.py
